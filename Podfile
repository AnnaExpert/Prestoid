# Prestoid v.1.3 remote repositories list

platform :ios, '10.0'

target 'prestoid' do
  
  use_frameworks!

  # Pods for prestoid
  
  pod 'SwiftyDropbox'
  pod 'Firebase/Core'
  pod 'Firebase/Crash'
  pod 'Firebase/Database'

end
